Optional icons for the ATC map overlay.
Place PNG files here (e.g. aircraft_friendly.png, aircraft_hostile.png, unit_friendly.png, unit_hostile.png, contact.png).
If a file is missing, the mod uses a simple built-in icon instead.
